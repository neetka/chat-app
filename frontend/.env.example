# Vite automatically loads .env files
# No additional environment variables needed for development
# Backend API is configured in src/lib/axios.js

# For production builds, configure your hosting platform's environment variables

# ENCRYPTION SECRET - IMPORTANT FOR PRODUCTION!
# This should be a long, random string (at least 32 characters)
# Both users in a conversation must have the same secret to decrypt messages
# Generate one with: openssl rand -base64 32
VITE_ENCRYPTION_SECRET=your-super-secret-encryption-key-change-this-in-production
